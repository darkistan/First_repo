# Користувач вводить дані
totalPages = int(input("Введіть загальну кількість сторінок: "))
donePages = int(input("Введіть кількість уже написаних сторінок: "))
daysLeft = int(input("Введіть кількість днів до дедлайну: "))

# Обчислення кількості сторінок, що залишились
pagesLeft = totalPages - donePages

# Обчислення, скільки треба писати щодня
if daysLeft > 0:
    pagesPerDay = pagesLeft / daysLeft
else:
    pagesPerDay = pagesLeft  # якщо 0 днів — просто показує скільки лишилось

print("Треба писати по", round(pagesPerDay, 2), "сторінок на день.")

# Поради
if pagesPerDay <= 2:
    print("😎 Все під контролем!")
elif 3 <= pagesPerDay <= 5:
    print("💪 Трішки зусиль — і все буде добре!")
else:
    print("⚡ Пора діяти швидше!")
